<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cyh.mybatis.dao.EmployeeMapper">

    <select id="getEmpById" resultType="com.cyh.mybatis.bean.Employee">
		select id, last_name lastName, email, gender from tbl_employee where id = #{id}
	</select>

    <insert id="batchInsertAndGenerateId" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO tbl_employee(last_name, email, gender)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.lastName}, #{item.email}, #{item.gender})
        </foreach>
    </insert>

</mapper>